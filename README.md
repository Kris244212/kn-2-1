# Cafe Booking System

Small demo project for managing cafe table bookings.

## Quickstart

- Create a virtual environment: `python -m venv venv`
- Install deps: `pip install -r requirements.txt`
- Run: `python main.py`

